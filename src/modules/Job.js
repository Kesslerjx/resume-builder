
export default class Job {
    constructor(name, title, from, to, tasks) {
        this.name  = name;
        this.title = title;
        this.from  = from;
        this.to    = to;
        this.tasks = tasks;
    }
}